PORT=5000
MONGO_URI=mongodb+srv://kavadehms:KNH%407812wer@cluster0.ultp4xn.mongodb.net/hospitalDB?retryWrites=true&w=majority
JWT_SECRET=xris9v2u8w5y7z!@#$%^&*()_+|~`xfo
NODE_ENV=production

# Single origin (backward-compatible):
ALLOWED_ORIGIN=http://localhost:3000

# Multi-origin (preferred for preview + prod URLs):

# If backend serves frontend/dist itself, set true.
# For split hosting (e.g., Vercel + Render), keep false.
SERVE_FRONTEND=false

# Request body limits
JSON_BODY_LIMIT=1mb
URLENCODED_BODY_LIMIT=1mb

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=300
AUTH_RATE_LIMIT_MAX=20
